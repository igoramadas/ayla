<aylaPhone:BasePage
    x:Class="AylaPhone.HomeLightsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    xmlns:aylaPhone="clr-namespace:AylaPhone"
    mc:Ignorable="d"
    shell:SystemTray.IsVisible="True"
    Loaded="Page_Loaded">

    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel x:Name="TitlePanel" Grid.Row="0" Style="{StaticResource TitlePanel}">
            <TextBlock x:Name="TopTitle" Style="{StaticResource TopTitle}" />
            <TextBlock x:Name="PageName" Text="Lights" Style="{StaticResource PageName}"/>
        </StackPanel>

        <StackPanel x:Name="ContentPanel" Grid.Row="1" Style="{StaticResource ContentPanel}">
            <ListBox></ListBox>
        </StackPanel>
    </Grid>

</aylaPhone:BasePage>